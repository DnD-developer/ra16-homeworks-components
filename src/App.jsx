import React from "react"
import ReactDOM from "react-dom/client"
import PageInternetMarket from "./PageInternetMarket"

ReactDOM.createRoot(document.querySelector("#root")).render(
	<React.StrictMode>
		<PageInternetMarket />
	</React.StrictMode>
)
